# ROI x-coordinates (y-coordinates from tw_kinetix_roi_row)
# Note: When load_roi=True (default), roi_x is loaded from standard .npy files
# Note: x-coordinates must be within image width (0 to 2400 pixels for Kinetix)
roi_x: [1200, 1450]  # Optional, only used when load_roi=False

# Optional site ROIs - only used when load_roi=False
# When load_roi=True (default), these are loaded from .npy files instead
# Note: All coordinates must be within image dimensions:
# - x-coordinates: 0 to 2400 pixels (Kinetix width)
# - y-coordinates: 0 to 2400 pixels (Kinetix height)
site_roi:
  site_roi_x: [[1220, 1430], [1240, 1410]]  # X ranges for each tweezer site
  site_roi_y: [[1080, 1100], [1080, 1100]]  # Y ranges for each tweezer site